# fff_print Legacy UT Migration Inventory

This document tracks migration of legacy `tests/fff_print` coverage into focused smoke targets.

Policy:
- Preserve production behavior. Test migration must not change business logic.
- Split by real ownership boundaries, not just to make tests easy.
- Migrating a legacy case into the repository does not automatically mean it belongs in the PR gate.
- Keep fixture-heavy G-code export and performance checks out of PR smoke until they have a separate manual/nightly target.

Status key:
- `done`: Migrated into a focused smoke target and verified locally.
- `partial`: Important coverage exists, but legacy scenarios are not fully migrated.
- `pending`: Candidate for migration.
- `manual/nightly`: Valuable, but too heavy or fixture-dependent for the fast PR gate.
- `skip`: Disabled, placeholder, obsolete, or not meaningful as an executable regression.

## Current Smoke Targets

| Target | Boundary | Current role |
|---|---|---|
| `libslic3r_config_tests` | config-core rules that do not require `Print::apply()` or full print state | Fast PR smoke |
| `placeholder_parser_smoke_tests` | `PlaceholderParser` template expansion for G-code context values without full export | Fast PR smoke under the placeholder parser path gate |
| `gcodewriter_smoke_tests` | `GCodeWriter`, `Extruder`, lightweight G-code coordinate state, writer-local config/state | Fast PR smoke |
| `gcode_reader_smoke_tests` | `GCodeReader` parser command, coordinate, extrusion/travel, comment, and tool-selection state | Fast PR smoke under the lightweight G-code path gate |
| `model_basic_smoke_tests` | `Model` assembly, placement, `TriangleMeshBasic` | Fast PR smoke |
| `print_filament_mapping_smoke_tests` | print filament mapping and config-facing map updates | Fast PR smoke |
| `print_apply_validate_smoke_tests` | `Print::apply()` / `Print::validate()` initialization invariants without full slicing | Fast PR smoke when apply/validate paths change |
| `print_perimeters_stage_smoke_tests` | perimeter generation stage | Fast PR smoke |
| `print_process_math_smoke_tests` | Flow math and `ExtrusionEntityCollection` flattening | Fast PR smoke |
| `print_process_core_smoke_tests` | solid layer re-slice behavior that still needs the heavy print core | Fast PR smoke when core print process paths change |
| `print_object_layers_smoke_tests` | object layer height generation | Fast PR smoke when object layer/slicing paths change |
| `print_adhesion_smoke_tests` | skirt/brim adhesion generation | Fast PR smoke when adhesion paths change |
| `fill_smoke_tests` | fill geometry and path generation | Fast PR smoke when fill paths change |
| `support_material_layers_smoke_tests` | support generator layer helper z/height initialization | Fast PR smoke when support layer helper paths change |
| `support_material_smoke_tests` | support material layer generation | Fast PR smoke when support paths change |
| `trianglemesh_geometry_smoke_tests` | TriangleMesh geometry, topology, primitive factories, and slicing | Fast PR smoke when geometry paths change |

## Legacy File Inventory

| Legacy file | Legacy scenario | Status | Target / future bucket | Notes |
|---|---|---|---|---|
| `test_gcodewriter.cpp` | `lift() is not ignored after unlift()` | done | `gcodewriter_smoke_tests` | Migrated as representative lift-state heights; the obsolete legacy source is no longer part of `fff_print_tests` because it calls removed `GCodeWriter` / `GCodeConfig` APIs. |
| `test_gcodewriter.cpp` | `set_speed emits values with fixed-point output` | done | `gcodewriter_smoke_tests` | Migrated; the current smoke file is the maintained coverage for the modern writer API. |
| `test_gcode.cpp` | `Origin manipulation` | done | `gcodewriter_smoke_tests` | Migrated through the lightweight `GCodeOriginState` boundary used by `GCode::set_origin()`, avoiding the full `GCode.cpp` export dependency chain while preserving the original origin-change translation behavior. The obsolete legacy source remains out of `fff_print_tests`. |
| `test_model.cpp` | `Model construction` | done | `model_basic_smoke_tests` | Migrated via object, volume, instance, mesh preservation checks; the obsolete legacy source is no longer part of `fff_print_tests` because its remaining print/export assertion still crosses full print application and failed on the current bed-fit path. |
| `test_trianglemesh.cpp` | basic mesh statistics | done | `model_basic_smoke_tests`, `trianglemesh_geometry_smoke_tests` | Migrated within `TriangleMeshBasic` boundary for volume, facet count, size, bounds, and vertex/facet arrays; `center()` is covered in the geometry target because it widens the light model-basic link boundary through `BoundingBox3Base`. The obsolete legacy source is no longer part of `fff_print_tests` because all maintained cases are now covered by smoke targets and the old exact area equality is float-precision fragile on Windows. |
| `test_trianglemesh.cpp` | translation / cube factory basics | done | `model_basic_smoke_tests` | Added within `TriangleMeshBasic` boundary, including translated bounds and the legacy single-origin-vertex cube factory representative. |
| `test_trianglemesh.cpp` | transformation functions | done | `trianglemesh_geometry_smoke_tests` | Migrated and path-filtered into PR smoke, including axis scale-up/down, vector/double translation, rotation, and origin alignment representatives. |
| `test_trianglemesh.cpp` | slice behavior | done | `trianglemesh_geometry_smoke_tests` | Migrated and path-filtered into PR smoke. |
| `test_trianglemesh.cpp` | cylinder/sphere factory helpers | done | `trianglemesh_geometry_smoke_tests` | Migrated and path-filtered into PR smoke, including primitive topology counts and approximate volumes. |
| `test_trianglemesh.cpp` | split / merge / cut behavior | done | `trianglemesh_geometry_smoke_tests` | Migrated and path-filtered into PR smoke. |
| `test_trianglemesh.cpp` | issue #4486 performance/profile tests | manual/nightly | nightly regression bucket | Performance guard; not PR smoke. |
| `test_flow.cpp` | non-bridge flow math | done | `print_process_math_smoke_tests` | Migrated into light math target; the obsolete legacy source is no longer part of `fff_print_tests` because remaining export-specific setup references removed config key `first_layer_extrusion_width`. |
| `test_flow.cpp` | bridge flow math | done | `print_process_math_smoke_tests` | Migrated into light math target; the old source also contains empty bridge-flow placeholders that should not be kept as executable coverage. |
| `test_flow.cpp` | 0.25mm nozzle auto-width edge cases | done | `print_process_math_smoke_tests` | Migrated into light math target. |
| `test_flow.cpp` | disabled solid spacing non-zero edge case | done | `print_process_math_smoke_tests` | Migrated against the current `Fill::_adjust_solid_spacing` helper boundary; the old `Flow::new_from_spacing` API no longer exists. |
| `test_flow.cpp` | extrusion width specifics through G-code export | manual/nightly | future G-code export target | Depends on full G-code generation. |
| `test_flow.cpp` | bridge flow specifics placeholders | skip | none | Empty placeholder sections. |
| `test_extrusion_entity.cpp` | collection flattening | done | `fff_print_tests`, `print_process_math_smoke_tests` | Migrated into light math/data-structure target. The legacy residual target still keeps this low-cost case because it is stable and does not cross print/export boundaries. |
| `test_printobject.cpp` | object layer heights | done | `print_object_layers_smoke_tests` | Migrated and split from print process core by object-layer responsibility; the obsolete legacy source is no longer part of `fff_print_tests` because it still initializes through the old full-print helper path and failed on current bed-fit validation. |
| `test_printobject.cpp` | disabled nozzle/max layer-height limit block | skip | none | Legacy block was already disabled; current layer-height generation does not preserve that obsolete fixed-count assumption, so do not change production behavior to satisfy it. |
| `test_print.cpp` | perimeter generation | done | `print_perimeters_stage_smoke_tests` | Migrated as stage executable smoke; the obsolete legacy source is no longer part of `fff_print_tests` because remaining brim assertions reference removed `Print::brim` API. |
| `test_print.cpp` | skirt generation | done | `print_adhesion_smoke_tests` | Migrated with adhesion scenarios and split from print process core. |
| `test_print.cpp` | solid surface re-slice classification | done | `print_process_core_smoke_tests` | Migrated. |
| `test_print.cpp` | brim generation | done | `print_adhesion_smoke_tests` | Migrated with adhesion scenarios and split from print process core. |
| `test_skirt_brim.cpp` | print-core skirt/brim geometry cases | done | `print_adhesion_smoke_tests` | Migrated representative adhesion and config edge cases, including generated skirt loops, large brim vs skirt, disabled skirt height, brim width/line-width scaling, and large minimum skirt length; the obsolete legacy source is no longer part of `fff_print_tests` because remaining cases are export/manual or reference removed `Print::brim` API. |
| `test_skirt_brim.cpp` | skirt height across G-code layers | manual/nightly | future G-code export smoke | Requires full G-code export and parser speed/layer inspection; keep out of PR smoke until a stable export harness exists. |
| `test_skirt_brim.cpp` | brim generated in G-code | manual/nightly | future G-code export smoke | Requires G-code export/parser inspection; current print-core smoke covers brim geometry directly instead. |
| `test_skirt_brim.cpp` | brim tool-selection through exported G-code | skip/manual | future G-code export smoke only if behavior is revalidated | Legacy cases are inside `#if 0` and note a real historical mismatch; do not revive without product confirmation and a stable export harness. |
| `test_skirt_brim.cpp` | brim ears cases | skip | none | Legacy cases are disabled and current config/behavior boundary is not established for PR smoke. |
| `test_skirt_brim.cpp` | overhang support plus brim exports G-code | manual/nightly | future G-code export smoke | Crosses support + adhesion + export; too broad for PR smoke. |
| `test_skirt_brim.cpp` | support-containing skirt length parser check | skip/manual | future export/support regression only if completed | Legacy block is disabled and marked unfinished; do not migrate as an executable regression yet. |
| `test_fill.cpp` | rectilinear path length / hole avoidance | done | `fill_smoke_tests` | Migrated and path-filtered into PR smoke. |
| `test_fill.cpp` | missing infill segment regression | done | `fill_smoke_tests` | Migrated and path-filtered into PR smoke. |
| `test_fill.cpp` | rotated square fill | done | `fill_smoke_tests` | Migrated and path-filtered into PR smoke. |
| `test_fill.cpp` | adjusted solid distance | done | `print_process_math_smoke_tests` | Migrated against current `Fill::_adjust_solid_spacing` helper boundary, including the legacy 250/47 representative and a non-zero narrow-width regression. |
| `test_fill.cpp` | solid surface fill helper cases | partial | `fill_smoke_tests` | Migrated narrow representative; the obsolete legacy source is no longer part of `fff_print_tests` because the large direct-fill representative is unstable against current Fill internals. A 2026-06-08 local trial inside `fill_smoke_tests` compiled, but `solid_surface_fully_filled(expolygon, 0.55, 0.0, 1.0f)` returned false against current production behavior, so keep it out of PR smoke unless a product bugfix intentionally changes Fill output. |
| `test_fill.cpp` | legacy path chaining comment block | done | `print_process_math_smoke_tests` | Migrated the lightweight `ExtrusionEntityCollection::chained_path_from()` representatives from the old Perl comment block into the current extrusion-entity boundary. The horizontal fixture is asserted against current nearest-path behavior rather than the obsolete exact order from the comment, preserving production logic. |
| `test_support_material.cpp` | raft layer count | done | `support_material_smoke_tests` | Migrated and path-filtered into PR smoke; the obsolete legacy source is no longer part of `fff_print_tests` because remaining checks use old config access patterns and incomplete support internals. |
| `test_support_material.cpp` | support layer Z/contact distance | done | `support_material_smoke_tests` | Migrated stable first-Z, thickness-bound, cube-with-hole, and zero top-contact-distance representatives. The obsolete top-spacing internals remain manual/nightly because the legacy assertion was disabled and depended on unavailable internal `support_z` state. |
| `test_support_material.cpp` | forced support / bridge speed disabled block | skip/manual | none or nightly | Currently disabled / incomplete. |
| `test_printgcode.cpp` | basic G-code output structure | manual/nightly | future G-code export smoke | Valuable, but should not be mixed into current PR smoke; keep out of `fff_print_tests` until a proper export harness owns the full-export dependency and Windows compile/runtime behavior. |
| `test_printgcode.cpp` / `test_skirt_brim.cpp` | parser-only final Z, complete-object Z reset/order, extrusion/travel, comments, between-object marker, and tool-selection checks | done | `gcode_reader_smoke_tests` | Migrated with synthetic G-code through the `GCodeReader` responsibility boundary. Full export generation and parser inspection against real exported G-code remain manual/nightly until a preset-backed export harness exists. |
| `test_printgcode.cpp` | layer/current-extruder placeholder expansion from exported custom G-code | done | `placeholder_parser_smoke_tests` | Migrated through the `PlaceholderParser` responsibility boundary by injecting the same context values that `GCode` passes during export (`layer_num`, `layer_z`, and `current_extruder`). This preserves the legacy placeholder coverage without requiring `Print::export_gcode()`. |
| `test_printgcode.cpp` | complete objects, support material, and real exported G-code text structure | manual/nightly | future G-code export smoke | Heavy export coverage; keep separate behind the same export harness boundary and out of the current legacy full target. |
| `test_data.cpp` | single-mesh `init_print` model/print setup | done | `print_apply_validate_smoke_tests` | Migrated as a direct apply/validate initialization invariant without full slicing or G-code export; the helper source is no longer part of the residual `fff_print_tests` executable, and its embedded legacy scenario remains compile-gated for any future explicit helper-test target. |
| `test_data.cpp` | `print.process()` and G-code output through helpers | manual/nightly | future G-code export smoke | Helper coverage crosses into full slicing/export; keep out of PR smoke. |

## Post-Migration Responsibility Splits

| Area | Status | Target / future bucket | Notes |
|---|---|---|---|
| Filament mapping pure rules | done | `libslic3r_config_tests` | `Print::is_dynamic_group_reorder()` and `Print::get_available_filament_map_modes()` now delegate their pure decisions to `FilamentMappingRules`, keeping the existing `Print` API behavior while covering those rules in the lighter config-core target. `PrintFilamentMappingRules*` changes are owned by config fast path filters rather than the heavier Print integration smoke. |
| Filament mapping print state/cache | done | `print_apply_validate_smoke_tests`, `print_filament_mapping_smoke_tests` | `PrintFilamentState.cpp` owns the simple filament map getters and `Print::update_filament_self_index_cache()`, preserving the existing `Print` API behavior while letting apply/validate avoid compiling full `PrintFilamentMapping.cpp`, config sync, and mapping rules. Changes to this file are included in the print smoke path filters that consume `Print::apply()` or filament map state. |
| Filament mapping config synchronization | partial | `libslic3r_config_tests`, `print_filament_mapping_smoke_tests` | `FilamentMappingConfigSync::sync_maps_to_config()` now owns the config synchronization rules in config-core, including explicit volume/nozzle maps, default volume-map derivation, and propagation to the original/full config outputs. `Print::update_filament_maps_to_config()` remains covered by the heavier mapping smoke as the integration API for placeholder parser updates, cache refresh, and auto-map state. The heavy mapping path filter is scoped to the `Print` API integration surface; `PrintFilamentCompatibility.cpp` changes are covered by apply/validate smoke instead. |
| G-code origin state | done | `gcodewriter_smoke_tests` | `GCodeOriginState` owns the coordinate-origin state transition and returns the scaled translation that `GCode::set_origin()` applies to `m_last_pos` and wipe path. This keeps the legacy origin manipulation coverage in PR smoke without linking full G-code export code. |
| G-code reader parser state | done | `gcode_reader_smoke_tests` | `libslic3r_gcode_reader_core` owns parser-only coverage extracted from legacy export tests: final Z tracking, complete-object Z reset detection, extrusion/travel classification, comments, and tool selection. This keeps high-value parser regressions in PR smoke without linking `Print::export_gcode()` or changing export behavior. |
| Placeholder parser G-code context | done | `placeholder_parser_smoke_tests` | `libslic3r_placeholder_parser_core` owns legacy G-code placeholder checks for `layer_num`, `layer_z`, and `current_extruder` by testing `PlaceholderParser` with the context values production `GCode` injects during export. This avoids full G-code export while preserving the stable macro-expansion behavior. |
| Print apply/validate initialization | done | `print_apply_validate_smoke_tests` | `test_print_init_smoke.cpp` now runs against real `PrintApply.cpp` and `PrintValidate.cpp`; `Print::process()` remains guarded by apply/validate compat so the target cannot silently become full slicing coverage. |
| Print apply/validate core boundary audit | done | `print_apply_validate_smoke_tests` | 2026-06-08 local CMake-only trial to derive this target directly from `libslic3r_print_object_layers_core` was rejected: real `Print::apply()` / `Print::validate()` still need model diff, print state, filament state, sequential validation, placeholder/config support, and runtime utility symbols. Continue reducing this target only after those responsibilities are extracted into production-owned units; do not replace them with test-only stubs or drag runtime logging/HTTP/crypto into the smoke boundary. |
| Print apply/validate support compat | done | `print_apply_validate_smoke_tests`, `print_filament_mapping_smoke_tests` | `PrintApplyValidateSupportCompat.cpp` owns the non-slicing stubs needed to link real `Print::apply()` / `Print::validate()` and destroy `PrintObject` safely without pulling cache export, output filename generation, wipe tower generation, or fill generator implementations into apply/validate-style smoke targets. This lets `print_filament_mapping_smoke_tests` derive from the apply/validate boundary instead of the heavier perimeters boundary. |
| Layer lifecycle cleanup | done | `print_apply_validate_smoke_tests`, `print_object_layers_smoke_tests`, `print_perimeters_stage_smoke_tests`, `print_process_core_smoke_tests` | `Layer::~Layer()` now lives in `LayerLifecycle.cpp`, separating object cleanup from the heavy `Layer.cpp` perimeter/fill implementation. This lets apply/validate keep real `PrintObject` destruction without compiling perimeter/Arachne layer-processing sources into that target. |
| Print solid-layer re-slice classification | done | `print_process_core_smoke_tests` | `test_print_solid_layers_smoke.cpp` keeps the full `Print::process()` path because the regression is about solid fill classification after slicing and a second `Print::apply()`. Its PR path filter is limited to the print apply/process, object slicing/layer, fill/flow, surface, model, triangle mesh, and config surfaces that can affect that assertion; output filename, print statistics, wipe tower, and base-output changes should not trigger this heavy smoke by themselves. |
| Print process core runtime boundary | done | `print_process_core_smoke_tests` | 2026-06-08 review confirmed this is still a true full-process boundary: the smoke initializes a real cube print, runs `Print::process()` twice, and asserts `LayerRegion::fill_surfaces` solid classification after re-slicing. Do not replace it with helper-only coverage unless production ownership changes; continue reducing PR cost through path filters and only split further when a real lower-level responsibility is extracted. |
| Fill geometry core | done | `fill_smoke_tests` | `libslic3r_fill_core` owns migrated Fill geometry coverage without linking the full print process core. It derives from the lightweight process-math core and adds Fill implementations plus their direct geometry dependencies (`EdgeGrid`, `VariableWidth`, Arachne wall paths, `Clipper2Utils`, `SVG`, and PNG debug support), preserving production Fill behavior while keeping `Print::process()` / `PrintObjectInfill.cpp` outside this target. |
| Fill spacing path ownership | done | `print_process_math_smoke_tests` | `FillSpacing.cpp` owns the pure `Fill::_adjust_solid_spacing()` helper and is covered by the process-math smoke representatives migrated from legacy Fill/Flow tests. The heavy `print_process_core_smoke_tests` path filter intentionally lists Fill generation files explicitly instead of `src/libslic3r/Fill/*`, so spacing-only helper changes do not trigger the full-process solid-layer re-slice smoke. |
| TriangleMesh geometry core | done | `trianglemesh_geometry_smoke_tests` | `libslic3r_trianglemesh_geometry_core` owns migrated TriangleMesh transform, slicing, primitive factory, split/merge, and cut coverage without linking the full print process core. The target keeps mesh/geometry dependencies such as `TriangleMesh.cpp`, `TriangleMeshSlicer.cpp`, `MeshDiagnostics.cpp`, `Tesselate.cpp`, medial-axis/Voronoi helpers, and the `ShortestPath` static dependencies needed by `ClipperUtils`, while excluding `Print::process()`, `PrintObjectInfill.cpp`, G-code, and support generation. |
| Print adhesion runtime boundary | done | `print_adhesion_smoke_tests` | 2026-06-08 review confirmed this remains a real `Print::process()` `psSkirtBrim` boundary: the smoke initializes real cube prints and asserts `Print::skirt()` / `Print::get_brimMap()` results after the skirt/brim stage runs. Do not replace it with helper-only coverage unless skirt/brim generation is first extracted into a production-owned stage API; path-filter tuning is safer than fake target splitting here. |
| Support material runtime boundary | done | `support_material_smoke_tests` | 2026-06-08 review confirmed this remains a real full-support boundary: the smoke initializes real prints, runs `Print::process()`, and asserts `PrintObject::support_layers()` for raft and cube-with-hole support scenarios. It covers `SupportMaterial::generate()`, raft/support layer construction, generated support layers, and toolpath preparation through production flow; keep it full-process unless a stable production support-core API is extracted. |
| Support material layer helpers | done | `support_material_layers_smoke_tests`, `support_material_smoke_tests` | `layer_z()`, `layer_initialize()`, and `layer_allocate()` now live in `SupportLayerUtils.hpp`, a light support-layer helper boundary. The new smoke target covers z/height initialization without pulling `Print::process()`, while the existing support material smoke keeps full raft/support generation coverage. |

## Next Migration Candidates

1. Remaining `Fill` large solid surface helper case: keep manual unless current Fill behavior is intentionally updated; the narrow representative is in `fill_smoke_tests`, and solid-spacing adjustment representatives are in the lighter `print_process_math_smoke_tests`. 2026-06-08 local validation showed the large representative currently fails at 0-degree orientation without changing production code.
2. `SupportMaterial` top-spacing internals: keep manual/nightly unless a stable support-core harness can inspect those layers without broad export or brittle geometry assumptions; the cube-with-hole layer-bound and zero top-contact-distance representatives are now in PR smoke.
3. `PrintGCode` export checks and the export half of `Model construction` / `test_data` helpers: require a proper G-code core/export harness first; direct full-export linkage is too broad and currently unsafe for PR smoke. `GCode` origin manipulation is already covered by the lightweight `GCodeOriginState` boundary in `gcodewriter_smoke_tests`, parser-only final-Z/object-reset/comment/tool-selection coverage is in `gcode_reader_smoke_tests`, and stable placeholder expansion is in `placeholder_parser_smoke_tests`.
4. Remaining `Skirt/Brim` G-code export/parser checks: keep manual/nightly or skip as documented above because they still require real exported skirt/brim/support G-code, not just `GCodeReader` parser state. Print-core geometry representatives are already in PR smoke.
5. Filament mapping config synchronization: config sync rules are now extracted into `PrintFilamentMappingConfigSync`; continue reducing the heavy mapping target only after the remaining `Print::apply()` integration responsibilities are isolated by real ownership.
6. With the stable legacy items now migrated or explicitly classified, continue runtime reduction with target-level path filter tuning and CI timing evidence.

## G-code Export Harness Investigation

2026-06-08 local investigation attempted a first `gcode_export_smoke_tests` target for the stable legacy `test_printgcode.cpp` coverage:

- Candidate assertions were limited to non-empty cube export, max-Z parsing through `GCodeReader`, and stable start/end custom G-code placeholder expansion.
- The target necessarily linked full `libslic3r`; a clean local build of that target took about 10 minutes even before runtime assertions, confirming it does not belong in Windows PR fast smoke.
- The old `Slic3r::Test::init_print()` helper is not safe for this fork's current export path because its legacy arrange/default-bed assumptions can throw `Objects could not fit on the bed`.
- Rebuilding the test with direct `Print::apply()` / `Print::validate()` initialization moved the failure into `Print::export_gcode()`, where a bare `DynamicPrintConfig::full_print_config()` crashes while serializing the full config block through `GCode::append_full_config()`.
- Conclusion: do not add a CTest target around full G-code export until the harness loads a real printer/process/filament preset bundle, or a separate production bugfix makes bare full-config export safe. This remains a manual/nightly bucket, not a PR fast migration candidate.

## Manual / Future Targets

| Future target | Moves from | Purpose |
|---|---|---|
| `gcode_export_smoke_tests` | `test_printgcode.cpp` candidates | Manual/nightly G-code export regressions; must be preset-backed rather than `full_print_config()`-only. |
