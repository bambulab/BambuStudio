# 设备研发本地编译规则（已迁移）

> [!IMPORTANT]
> 本文件内容已整体迁移为技能：
> `/openspec/skills/device/09-设备研发本地编译技能.md`
>
> 对应 Cursor 技能桥接：`/.cursor/skills/device/device-local-build/SKILL.md`
>
> 本文件保留一段 stub，仅为了避免既有文档（如 `openspec/specs/device/` 下的 tasks.md）中的路径引用失效。

## 变更摘要

- 原"本地编译规则"的全部约束（Release 档位、`libslic3r_gui` 必须 `/Od /Zi`、两档编译、验证门槛、红线）原文迁入技能；
- 新增**触发门控**：本技能**不随 `deviceDevelopProfile` 开关自动激活**，必须由用户手动调用或明确说"按设备域本地编译技能进行编译"；
- 新增**触发告知**：激活时必须先向用户输出声明（档位 / 目标 / 耗时 / 将执行命令），等待确认后再编译。

## 如何引用

- 内部文档推荐直接引用技能路径：`/openspec/skills/device/09-设备研发本地编译技能.md`
- 旧路径 `/openspec/rules/device/13-设备研发本地编译规则.md` 仍可读，但仅作为指路 stub，不再承载具体约束
